'use client';

type MonetagSlotProps = {
  zoneId?: string;
  className?: string;
  label?: string;
};

export default function MonetagSlot(_props: MonetagSlotProps) {
  // Monetag disabled – render nothing.
  return null;
}
