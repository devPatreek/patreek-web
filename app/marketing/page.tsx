import LandingPage from '@/components/LandingPage';

/**
 * Marketing homepage for patreek.com
 * This will be served when user visits patreek.com (via www alias)
 * Can be used later when setting up domain-based routing
 */
export default function MarketingPage() {
  return <LandingPage />;
}

